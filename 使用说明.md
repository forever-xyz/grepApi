# Grep API 使用说明

## 安装插件

1. 打开 IntelliJ IDEA 2024.1 或更高版本。
2. 进入 `File | Settings | Plugins`。
3. 点击 Plugins 页面右上角的齿轮按钮。
4. 选择 `Install Plugin from Disk...`。
5. 选择项目中的安装包：

   ```text
   build/distributions/GrepApi-0.0.1.zip
   ```

6. 安装完成后，按照 IDEA 提示重启。

不要解压 ZIP，直接在 IDEA 中选择 ZIP 文件安装。

## 搜索接口

打开一个 Java Spring 项目，等待 IDEA 完成项目索引，然后使用任意一种方式打开搜索窗口：

- Windows/Linux：按 `Ctrl+Alt+/`
- macOS：按 `Command+Option+/`（`⌘⌥/`）
- 点击 `Navigate | 搜索 API 接口...`
- 按 `Ctrl+Shift+A`，搜索 `搜索 API 接口`

支持以下输入：

```text
/api/users/123
GET /api/users/123
https://api.example.com/api/users/123?detail=true
curl -X POST "https://api.example.com/api/users"
```

输入后会显示匹配的 HTTP 方法、完整路由、Controller、Java 方法、模块和匹配原因。

输入任意路径片段、方法名、Controller 名或模块名即可模糊搜索。使用上下键选择结果，按 Enter 或双击结果后，插件会打开对应 Java 文件，将光标移动到方法的 Mapping 声明，并选中对应路径。按 Esc 关闭搜索框。

输入以 `/` 开头的 URL 路径时，只匹配路径中连续包含该内容的接口；右侧下拉框可以继续按 GET、POST、PUT、DELETE、PATCH、HEAD 或 OPTIONS 筛选。

匹配到的路径文字会显示背景色，例如搜索 `confirm` 时只高亮结果路径中的 `confirm`。可在 `Settings | Tools | Grep API` 中选择蓝色、绿色、橙色或紫色，并查看实时预览。

## 配置网关前缀

如果日志中的 URL 包含网关或服务名前缀，例如：

```text
/gateway/user-service/api/users/123
```

进入：

```text
File | Settings | Tools | Grep API
```

每行填写一个需要移除的前缀：

```text
/gateway/user-service
/prod/user-service
```

配置后，上面的运行时 URL 可以匹配源码中的：

```java
@RequestMapping("/api/users")
@GetMapping("/{id}")
```

## 当前支持范围

- IntelliJ IDEA 2024.1 至 2026.x
- Community 和 Ultimate
- Java Spring MVC/WebFlux 注解式接口
- `@RequestMapping`
- `@GetMapping`
- `@PostMapping`
- `@PutMapping`
- `@DeleteMapping`
- `@PatchMapping`
- 类级和方法级路径合并
- `{id}`、`{id:\\d+}`、`*`、`**`
- Mapping 中的字符串常量
- 多模块结果展示

当前版本暂不支持 Kotlin、Spring Functional Routing、Ktor、JAX-RS 和自定义组合注解。

## 重新构建

插件以 IDEA 2024.1 和 Java 17 为最低兼容基线。如果本机已经安装 IDEA 2024，可以直接使用本地 IDEA SDK：

```powershell
.\\gradlew.bat buildPlugin "-PlocalIdePath=D:/software/IntelliJ IDEA 2024.1"
```

生成的安装包位于：

```text
build/distributions/
```
